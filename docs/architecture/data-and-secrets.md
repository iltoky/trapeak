# Данные, конфигурация и секреты

## Переменные окружения

Переменные окружения содержат конфигурацию экземпляра TRAPEAK, общую для всех пользователей:

```env
DATABASE_URL=
APP_URL=
AUTH_SECRET=
GARMIN_CLIENT_ID=
GARMIN_CLIENT_SECRET=
GARMIN_REDIRECT_URI=
TOKEN_ENCRYPTION_KEY=
```

Точные названия Garmin-параметров необходимо сверить в закрытой документации Developer Portal. В Git хранится только `.env.example` без значений.

## Данные пользователя

Для каждого пользователя в БД хранится отдельное подключение Garmin:

- внутренний `user_id`;
- Garmin user identifier;
- статус подключения;
- access/refresh tokens в зашифрованном виде, если соответствующие токены предусмотрены Garmin;
- срок действия и scopes;
- даты подключения, ошибки и отключения.

Пароль Garmin не запрашивается и нигде не хранится. OAuth-токены нельзя хранить в переменных окружения: это динамические данные конкретных пользователей.

## Правила

- шифрование токенов выполняется на сервере;
- ключ шифрования хранится отдельно от БД;
- authorization code и токены не логируются;
- raw OAuth responses не сохраняются;
- удаление аккаунта, отключение Garmin и удаление тренировок — разные операции.

