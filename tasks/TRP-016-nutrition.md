# TRP-016 — Nutrition journal and MCP

Статус: **Published; authenticated production e2e passed**

## Описание

Добавить пользовательский журнал питания с ручным вводом калорий и макронутриентов. Данные доступны только владельцу в dashboard и подключённым им MCP-клиентам.

## Acceptance criteria

- Пользователь может сохранить приём пищи с датой/временем, типом, названием, калориями, белками, углеводами, жирами и необязательной заметкой.
- Сервер валидирует длину и числовые диапазоны независимо от browser validation.
- Пользователь видит последние записи и дневные итоги, а удалить может только собственную запись.
- MCP предоставляет `create_nutrition_entry`, `list_nutrition_entries`, `get_nutrition_summary` и owner-scoped `delete_nutrition_entry`.
- Удаление через MCP требует ID существующей записи и явного `confirm: true`; dashboard запрашивает подтверждение перед удалением.
- Все запросы к БД обязательно ограничены текущим Clerk user ID.
- Nutrition описывается как пользовательский журнал, а не медицинская рекомендация.

## Release actions

1. [x] Применить `0003_nutrition.sql` к production БД до публикации кода.
2. [x] Опубликовать commit `606369e6c481586388a32df147c4af1c96906fd1` и дождаться Vercel `READY`.
3. [x] Проверить AI-запись, список и дневные итоги через production MCP.
4. [x] Подтвердить, что повторная отправка не создаёт дубль.
5. После публикации v0.7.0 обновить schema подключённого AI-клиента и проверить удаление через MCP и dashboard.

## QA e2e tests

1. [x] Добавление корректного meal создаёт одну запись.
2. [x] Повторная AI-запись с теми же полями не создаёт дубль.
3. [x] Дневные итоги равны сумме записей соответствующего UTC-дня.
4. [ ] Пользователь не может удалить чужую запись.
5. [x] MCP-фильтры по датам возвращают только данные текущего пользователя.
6. [ ] `delete_nutrition_entry` без `confirm: true` отклоняется, а с подтверждением удаляет только запись владельца.
